let currentWallet = 1000;
const currentBet = document.getElementById ('current-bet')
const makeBet =  document.getElementById ('make-bet')


makeBet.addEventListener('click', () => {
    const number = Number(currentBet.value)
    const results = Math.random();
        if (results > 0.5) {
            
            
        }
    console.log(results)

})

